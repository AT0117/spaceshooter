import 'package:flutter/material.dart';

class GameConfig {
  static const double planetRadius = 40.0;
  static const double bulletSpeed = 600.0;
  static const double enemySpeed = 120.0;
  static const List<Color> bulletColorList = [
    Colors.greenAccent,
    Colors.cyanAccent,
  ];
}
